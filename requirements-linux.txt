# Dependências de execução — Ubuntu / Linux
# Versão atual: 1.7.1 (version.py)
# Instalar: pip install -r requirements-linux.txt
# Executar: python3 main.py
# Gerar instalador (.deb): ./ubuntu/build-deb.sh
# Gerar release notes: py new_release.py

PySide6>=6.5.0
pynput>=1.7.6
sounddevice>=0.4.6
numpy>=1.20.0
openai>=1.0.0
google-generativeai>=0.3.0
faster-whisper>=0.10.0
python-xlib>=0.33; sys_platform == 'linux'

# Ferramentas de build (opcional; ubuntu/build-deb.sh instala automaticamente)
pyinstaller>=6.0.0; sys_platform == 'linux'
