# Dependências de execução — Windows
# Versão atual: 1.7.1 (version.py)
# Instalar: py -m pip install -r requirements.txt
# Executar: py main.py
# Gerar instalador (.exe): py build-windows.py
# Gerar release notes: py new_release.py

PySide6>=6.5.0
pynput>=1.7.6
sounddevice>=0.4.6
numpy>=1.20.0
openai>=1.0.0
google-generativeai>=0.3.0
faster-whisper>=0.10.0
pycaw>=20251023; sys_platform == 'win32'
comtypes>=1.4.16; sys_platform == 'win32'

# Ferramentas de build (opcional; build-windows.py instala automaticamente)
pyinstaller>=6.0.0; sys_platform == 'win32'
pillow>=10.0.0; sys_platform == 'win32'
